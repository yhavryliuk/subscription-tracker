# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type SubscriptionGQL {
  id: ID!
  name: String!
  amount: Float!
  period: Period!
  status: Status!
  userId: ID!
}

enum Period {
  DAILY
  WEEKLY
  MONTHLY
  YEARLY
}

enum Status {
  ACTIVE
  INACTIVE
  CANCELLED
}

type UserGQL {
  id: ID!
  email: String!
  name: String!
  subscriptions: [SubscriptionGQL!]!
}

type SubscriptionConnection {
  items: [SubscriptionGQL!]!
  total: Int!
}

type SessionGQL {
  id: ID!
  userId: String!
  userAgent: String
  ip: String
  device: String
  createdAt: DateTime!
  updatedAt: DateTime!
  lastUsedAt: DateTime!
  revokedAt: DateTime
  isCurrent: Boolean!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Query {
  mySessions: [SessionGQL!]!
  me: UserGQL!
  users: [UserGQL!]!
  user(id: String!): UserGQL
  subscriptions(pagination: PaginationInput, filter: SubscriptionFilterInput): SubscriptionConnection!
  subscriptionsByUser(userId: String!): [SubscriptionGQL!]!
  mySubscriptions: [SubscriptionGQL!]!
}

input PaginationInput {
  limit: Int! = 10
  offset: Int! = 0
}

input SubscriptionFilterInput {
  status: Status
  period: Period
}

type Mutation {
  login(email: String!, password: String!): Boolean!
  refresh: Boolean!
  logout: Boolean!
  createUser(input: CreateUserInput!): UserGQL!
  addSubscription(input: CreateSubscriptionInput!): SubscriptionGQL!
}

input CreateUserInput {
  email: String!
  name: String!
}

input CreateSubscriptionInput {
  name: String!
  amount: Float!
  period: Period!
  status: Status!
}